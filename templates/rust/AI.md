# PROJECTNAME

Replace this line with the Rust application's or library's purpose and users.

Stable Rust and Cargo in a Nix shell. No crate is initialized. Run `cargo init` when creating it; replace `PROJECTNAME` here and in `flake.nix`.

## Commands

- `nix develop` — enter the shell.
- After initialization: `cargo check`, `cargo test`, `cargo clippy -- -D warnings`, `cargo fmt --check`.

After code exists, record crate entry points, workspace boundaries, verified commands, and project constraints.
