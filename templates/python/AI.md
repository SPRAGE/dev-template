# PROJECTNAME

Replace this line with the Python application's purpose and users.

Python 3.13 with `uv` in a Nix shell. No package is initialized. Run `uv init` when creating it; replace `PROJECTNAME` here and in `flake.nix`.

## Commands

- `nix develop` — enter the shell.
- `uv sync` — install dependencies after `pyproject.toml` exists.
- `uv run python --version` — verify the Python environment.

After code exists, record entry points, constraints, and declared test/lint/type-check commands. None are configured yet.
