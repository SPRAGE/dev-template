# PROJECTNAME

Replace this line with the project's purpose and users.

Replace `PROJECTNAME` in this file and `flake.nix`. The shell is language-neutral; no application exists yet.

## Commands

- `nix develop` — enter the shell.
- `nix flake check` — validate the flake.

After code exists, replace starter text with verified entry points, build/test/lint commands, and project constraints. Do not invent application commands.
